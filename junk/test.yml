---
- name: Example Simple Playbook
  hosts: localhost
  become: no
  
  tasks:
  - name: Execute the command in remote shell; stdout goes to the specified file on the remote
    ansible.builtin.shell: echo "foo" > /tmp/output.txt
